using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class kikkailuskripti : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Tämä file pitää ignoroida
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
